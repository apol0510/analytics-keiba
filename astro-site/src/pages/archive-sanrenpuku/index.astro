---
// 南関 三連複アーカイブのランディング。**301 リダイレクトのみ**。
//
// 実績は統合ページ /archive-sanrenpuku-all/ に集約済み（中央タブ／南関タブ）。
// 月別詳細は archive-sanrenpuku/[year]/[month].astro、年別は 2025/ ・ 2026/ が担当する。
// 中央側ランディング archive-sanrenpuku-jra/index.astro と同じ形。
//
// 【2026-09-02】ここにあった 917 行（会員ゲート・本文・style・per-month JSON の import）を削除した。
//   - 301 は以前から無条件で先頭にあり、**本文は一度も返していなかった**（到達しない死んだコード）。
//   - 残していた理由は月次メンテ 3 スクリプトが本ファイルの import 行を読んでいたことだったが、
//     per-month JSON（archiveSanrenpukuResults_YYYY-MM.json）は **2026-05 を最後に追加が止まり**、
//     新しい月は combined（archiveSanrenpukuResults.json）へ自動取込される運用に変わっている。
//     ＝ その月次手順自体が既に使われていないため、依存ごと整理した。
//   - 併せて scripts/validate-archive-data.js の参照先を「実際に使っているページ」へ更新し、
//     旧月次スクリプト 2 本は削除した（手順は MONTHLY_ARCHIVE_GUIDE.md に集約）。
//
// **本文を戻さないこと。** 戻すなら /archive-sanrenpuku-all/ 側に足す。
return Astro.redirect('/archive-sanrenpuku-all/', 301);
---
