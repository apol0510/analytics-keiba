---
/**
 * `/light-predictions-funabashi/` — **301 リダイレクトのみ**（2026-09-02）
 *
 * 理由・注意は `light-predictions-urawa.astro` と同じ。
 * サイト内から一切リンクされておらず、`/light-predictions/` が会場を問わず
 * 南関の最新開催日を表示するため、会場別ページは機能的に重複していた。
 *
 * ⚠️ ここに予想の描画を戻さないこと。`memberPredictionFunnel.guard.test.mjs` が
 *    「このファイルが純粋な 301 であること」を検査している。
 */
export const prerender = false;

return Astro.redirect('/light-predictions/', 301);
---
