---
// 旧URL(/free-prediction-jra/[date]/) → 新URL(/free-prediction/jra/[date]/) へのサーバ301。
// SSR化して日付セグメントを保持したまま確実に301を返す（静的stubのmeta-refresh/JS転送を廃止）。
export const prerender = false;
const { date } = Astro.params;
return Astro.redirect(`/free-prediction/jra/${date}/`, 301);
---
